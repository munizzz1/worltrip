import { extendTheme } from '@chakra-ui/react';

export const theme = extendTheme({
    colors: {
        lightInfo: 'rgba(218, 218, 218, 1)',
        darkHeadings: '#47585B',
    },
    fonts: {
        heading: 'Poppins',
        body: 'Poppins',
    }
});